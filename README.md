so basically I implemented my own neural network from scratch using some AI help for how i could make it better,
it has RELU as the activation function and loss tracking as well
model.py has the neural network model class coded and we use train.py to actually train the model and once training finishes it creates a pickle file which then can be used to draw our own digits in draw_digit.py
trained_network.pkl has around 98.3% accuracy and trained_network1.pkl aas 98.35% accuracy on testing data
we can use whichever one we like to draw our digits
